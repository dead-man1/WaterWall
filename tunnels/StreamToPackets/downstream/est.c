#include "structure.h"

#include "loggers/network_logger.h"

void streamtopacketsTunnelDownStreamEst(tunnel_t *t, line_t *l)
{
    discard t;
    discard l;

    // ok but still packet tunnels dont send est
}
